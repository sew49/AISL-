# Check Environment Variables in Docker Container

Run this command to see the environment variables inside the running container:

```
bash
docker exec attendance-staff env
```

This will display all environment variables set inside the `attendance-staff` container.
